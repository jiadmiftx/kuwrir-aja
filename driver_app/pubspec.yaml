name: kuwrir_driver
description: KUWRIR Driver App — Deliver food and earn money
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  kuwrir_shared:
    path: ../shared/kuwrir_shared
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
